<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.DarkActionBar" parent="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="colorButtonNormal">@color/colorTextIcon</item>
    </style>

    <style name="Heroes.Toolbar" parent="@style/ThemeOverlay.MaterialComponents.ActionBar">
        <item name="colorButtonNormal">@color/colorTextIcon</item>
    </style>

    <style name="Heroes.Card.Details" parent="Widget.MaterialComponents.CardView">
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="cardCornerRadius">4dp</item>
    </style>

</resources>
